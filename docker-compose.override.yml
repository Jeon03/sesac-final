# 개발 환경 (docker compose up 시 자동 적용)
services:
  backend:
    environment:
      - DEBUG=True
      - ALLOWED_HOSTS=localhost,127.0.0.1
    volumes:
      - .:/app
    command: python manage.py runserver 0.0.0.0:8000

  frontend:
    volumes:
      - .:/app
